/**
 * Shown in place of a photographer's studio name when she has set neither a
 * studio name nor a personal name — on her public site, client galleries,
 * portfolio, blog and as the default watermark. This is the PHOTOGRAPHER's
 * display identity, not the platform: it must stay neutral and must never
 * leak the platform brand (STG) onto a photographer's own pages.
 */
export const FALLBACK_STUDIO_NAME = 'הסטודיו'
